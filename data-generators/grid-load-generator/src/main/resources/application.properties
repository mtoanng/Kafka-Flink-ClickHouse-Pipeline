# =====================================================================
# Grid Load Generator (Pillar 3) — Kafka producer config
# =====================================================================
# Có thể override qua biến môi trường hoặc -Dproducer.interval.ms=...
kafka.bootstrap.servers=localhost:9092
kafka.topic=grid-load
producer.interval.ms=5000
